#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdbool.h>

// Structure of the employee
struct Element
{
    char name[50];
    char symbol[1];
    int atomic_no;
    float atomic_wt;
};

// Function to add an element record if it doesn't exist (by symbol)
bool add_record(struct Element e)
{
}

// Function to delete an element record matching symbol
bool delete_record(struct Element e)
{
}

// Function to modify an element record matching symbol
bool modify_record()
{
}

// Function to return element record(s) matching all members of e
bool get_records(struct Element e, bool match_all)
{
}





/*
 * Function:  main
 * --------------------
 * Main for periodic table of the elements
 *
 *
 */
int main()
{

    printf("test");

    struct Element e;

    return 0;
}